use cardiac_core::Assembler;
use std::collections::HashMap;

fn main() {
    let mut assembler: Assembler = Assembler::new();

    // assembler.next_step();

    // let code: HashMap<u32, u32> = HashMap::from([
    //     (21, 200),
    //     (23, 201),
    // ]);
    // assembler.load_program(code);

}
